## Testing

The Returns route was built using TDD, so any bugs are caught as soon as possible without having to test edge cases.

Test code with `npm test` which runs jest. Jest may not be the best testing package for mongoose, but it's what Mosh taught.
